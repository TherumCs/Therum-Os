# macOS / Google Drive noise
.DS_Store
.Spotlight-V100
.Trashes
Icon?
._*

# Nothing here should ever carry real credentials — see CLAUDE.md's
# "Never store secrets" rule — but belt and braces.
.env
.env.*
*.key
*.pem

# Backups live in the folder (Google Drive syncs them offsite) but never in git:
# a 90 MB zip per run would bloat the repo permanently, and the zip contains a
# full database dump — customer rows included.
backups/
