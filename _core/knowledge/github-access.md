---
name: github-access
description: What GitHub access exists on Bam's Mac (SSH deploy key only; no token, no gh login) and how release notes get published
metadata:
  type: reference
---

Checked 2026-09-16 (Bam thought "github auth is in the memory" — it was not):
- **SSH key works** as `TherumCs` (`ssh -T git@github.com`) → clone/push over `git@github.com:TherumCs/…` is fine. That is how every push in this project has worked.
- **No API token anywhere**: no `~/.config/gh` (gh CLI never logged in), no `GH_TOKEN`/`GITHUB_TOKEN` in env, no keychain entry for github.com, no `.netrc`, no credential helper. The `plugin:engineering:github` MCP connector is listed but needs OAuth Bam must authorise in claude.ai connector settings.
- Consequence: I can push commits + tags but cannot create a GitHub **Release** object. Workaround in use: release notes live in `CHANGELOG.md` in the repo (beta.10 entry pushed as `055f051`), so the tag page shows the code and the changelog carries the notes.
- Repos: `TherumCs/Therum-OS-2.0` (the product; tag `v2.0.0-beta.10`), `TherumCs/Therum-Os` (this ops folder).
- To publish a Release: Bam runs `gh auth login` on this Mac once (or authorises the GitHub connector) → then `gh release create v2.0.0-beta.10 --notes-from-tag` or from the changelog. Never paste a token into chat.

**Repo state as of 2026-09-16 (Bam: "has therum 2.0 been fully updated on git?").** Nothing had been committed since 2026-08-21 (`16a3a2a`) — every fix + all of Flow were uncommitted. Now: `TherumCs/Therum-OS-2.0` main = `2c68a30` "Therum OS 2.0.0-beta.10 — Flow + fixes since beta.9" (138 files, +11,937), tag `v2.0.0-beta.10`, package.json/admin bumped to beta.10. Ops repo `TherumCs/Therum-Os` (this TSC-BETA folder) main = `c6b86ce` (FAILURES log, memory, audits). `_core/` inside the app repo is gitignored (ops notes live in TSC-BETA). Box `/home/therum/therum` was on the beta.6 commit with a 194-file stale tree → `git reset --hard origin/main` (untracked dist/.env/uploads/admin/.next untouched), stray dist artifacts removed, admin rebuilt from main. Rule going forward: after each deploy day, commit + push; the box's source must equal origin/main because `admin` builds ON the box from source.
- 2026-09-16 "all the 2.0 stuff" inventory: 2.0 = ONE repo `Therum-OS-2.0` (src backend, admin Next, builder Vite 18 src files all tracked, prisma, shared tokens, deploy, docs, tools, tests). Box-only 2.0 pieces now captured under `deploy/live/` (live `sidemoney` + `sm-engine-internal` nginx vhosts, `sm-appliance.php` WooPayments bridge mu-plugin) + `docs/design/payments-redesign/` (Direction A/B/C mockups). NOT 2.0 / not in git on purpose: `Local Sites/therum-os/app` (WordPress 1.9.44, sunset), `conf/` (Local), `/var/www/sidemoney-static` (42 MB ported theme assets; holds the email/popup logo png), `/home/therum/forge` (separate Forge product, own ops folder), uploads/.env/DB (backups). `gh` CLI is NOT logged in on this Mac → no GitHub Release object; tag page is the link.
- 2026-09-16 (later): `v2.0.0-beta.10` tag was force-moved to the store-neutral commit `480fd70` (first cut `2c68a30` still had Sidemoney in code). `.env.example` now documents EMAIL_LOGO_URL / CAREERS_INBOX / SITE_PACK_DIR. Upgrade path for any other 2.0 site = DEPLOY.md "Rolling deploy": checkout tag → npm ci → prisma migrate deploy → build api + admin → pm2 reload ecosystem.config.cjs --update-env + restart worker/admin. 11 migrations since beta.9 (incl. marketing_module). Bam asked for the link to upgrade "my other site" — which site was not named.
